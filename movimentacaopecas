// Movimentação peças de xadrez (Nível Novato)
Aluna: Larissa Severo de Faria

#include <stdio.h>

int main() {
    // Mover a Torre 5 casas para a direita
    for (int a = 0; a < 5; a++) {
        printf("Direita\n"); // Imprime a direção do movimento
    }

    // Mover o Bispo 5 casas na diagonal
    int b = 0;
    while (b < 5) {
        printf("Cima, Direita\n"); // Imprime a direção do movimento
        b++; // Incrementa o contador para evitar loop infinito
    }

    // Mover a Rainha 8 casas para a esquerda
    int c = 0;
    do {
        printf("Esquerda\n"); // Imprime a direção do movimento
        c++; // Incrementa o contador para evitar loop infinito
    } while (c < 8);

    return 0;
}
